{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tlachiwalis — backend Express (monolito modular). Lógica sensible: checkout, pagos, retenciones, CFDI, dispersión.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2",
    "cors": "^2",
    "dotenv": "^16",
    "express": "^4",
    "stripe": "^22.3.0",
    "zod": "^3"
  },
  "devDependencies": {
    "@types/cors": "^2",
    "@types/express": "^4",
    "@types/node": "^20",
    "tsx": "^4",
    "typescript": "^5"
  }
}
